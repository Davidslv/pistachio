<%= form_with(model: admin_allowed_area) do |form| %>
  <% if admin_allowed_area.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(admin_allowed_area.errors.count, "error") %> prohibited this admin_allowed_area from being saved:</h2>

      <ul>
        <% admin_allowed_area.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :area, style: "display: block" %>
    <%= form.text_field :area %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
