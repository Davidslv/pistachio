<%= form_with(model: admin_allowed_postcode) do |form| %>
  <% if admin_allowed_postcode.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(admin_allowed_postcode.errors.count, "error") %> prohibited this admin_allowed_postcode from being saved:</h2>

      <ul>
        <% admin_allowed_postcode.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :postcode, style: "display: block" %>
    <%= form.text_field :postcode %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
